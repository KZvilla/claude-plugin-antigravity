# Auto detect text files and perform LF normalization
* text=auto

# Force LF for shell scripts (critical for Linux/macOS)
*.sh text eol=lf

# Force CRLF for PowerShell on Windows
*.ps1 text eol=crlf
