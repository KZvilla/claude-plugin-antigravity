---
description: Delegate any task to Antigravity (agy) — it reads and writes files
argument-hint: <task or prompt>
---

Delegate the following task to Google Antigravity CLI (`agy`):

$ARGUMENTS

Instructions:
1. Use the `mcp__antigravity__agy_run` tool to dispatch this prompt to Antigravity.
2. Set `effort: "high"` and `dangerously_skip_permissions: true`.
3. When Antigravity completes, present the result, highlight files changed or actions taken, and display the `conversation_id` for any follow-up turns.
